<?php
  if($form)
      echo "TENGO MODELO";
  else
      echo "NO";
  echo $name;
  $field=$form->__getField($name);
      
  $it1=$field->getIterator();
  echo "IT1 COUNT::".$it1->count()."<br>";
  $ds=$field->getInverseDatasource(); 
  $ds->initialize();
  echo "TENGO DS"; 
  $it=$ds->getIterator();
  echo "TENGO ITERADOR";
  echo $it->count();
  echo "FIN DE CUENTA";
  //$includedDs=
?>
