DEFINE projectPath "/var/www/adtopy"
<VirtualHost *:80>
    ServerName statics.adtopy.com
   DocumentRoot "${projectPath}/sites/statics/html"
    <Directory "${projectPath}/sites/statics/html">
     Options -Indexes +FollowSymLinks -MultiViews
     AllowOverride All
     Order allow,deny
     Allow from all
	 # Always set these headers.
	Header always set Access-Control-Allow-Origin "*"
	Header always set Access-Control-Allow-Methods "POST, GET, OPTIONS, DELETE, PUT"
	Header always set Access-Control-Max-Age "1000"
	Header always set Access-Control-Allow-Headers "x-requested-with, Content-Type, origin, authorization, accept, client-security-token"

	# Added a rewrite to respond with a 200 SUCCESS on every OPTIONS request.
	RewriteEngine On
	RewriteCond %{REQUEST_METHOD} OPTIONS
	RewriteRule ^(.*)$ $1 [R=200,L]
    </Directory>
</VirtualHost>
<IfModule php7_module>
</IfModule>

<VirtualHost *:80>
    ServerName reflection.adtopy.com
   DocumentRoot "${projectPath}/sites/reflection/html"

    <Directory "${projectPath}/sites/reflection/html">
    RewriteEngine on
#    RewriteCond %{REQUEST_METHOD} OPTIONS
#	RewriteRule ^(.*)$ $1 [R=200,L]

#	RewriteCond ${projectPath}/sites/reflection/$1 -f [OR]
#	RewriteCond ${projectPath}/sites/reflection/$1 -d
#	RewriteRule (.*) $0?site=reflection&%{QUERY_STRING} [L]

#    RewriteRule .* index.php?site=reflection&subpath=$0 [QSA,L]
	RewriteCond %{REQUEST_FILENAME} !(\.png|\.jpg|\.gif|\.jpeg|\.bmp)$
	RewriteRule .* index.php?site=reflection&subpath=$0 [QSA,L]

     Options -Indexes +FollowSymLinks -MultiViews
     AllowOverride All
     Order allow,deny
     Allow from all

	 # Always set these headers.
	Header always set Access-Control-Allow-Origin "*"
	Header always set Access-Control-Allow-Methods "POST, GET, OPTIONS, DELETE, PUT"
	Header always set Access-Control-Max-Age "1000"
	Header always set Access-Control-Allow-Headers "x-requested-with, Content-Type, origin, authorization, accept, client-security-token"

	# Added a rewrite to respond with a 200 SUCCESS on every OPTIONS request.


    </Directory>
</VirtualHost>


<VirtualHost *:80>
    ServerName editor.adtopy.com
   DocumentRoot "${projectPath}/sites/editor/html"
    RewriteEngine on

    <Directory "${projectPath}/sites/editor/html">

	RewriteCond ${projectPath}/sites/editor/html/$1 -f
	RewriteRule (.*) $1?site=editor&%{QUERY_STRING} [L]

    RewriteRule .* index.php?site=editor&subpath=$0 [QSA,L]

     Options -Indexes +FollowSymLinks -MultiViews
     #AllowOverride All
     #Order allow,deny
     #Allow from all

	 # Always set these headers.
	Header always set Access-Control-Allow-Origin "*"
	Header always set Access-Control-Allow-Methods "POST, GET, OPTIONS, DELETE, PUT"
	Header always set Access-Control-Max-Age "1000"
	Header always set Access-Control-Allow-Headers "x-requested-with, Content-Type, origin, authorization, accept, client-security-token"

	# Added a rewrite to respond with a 200 SUCCESS on every OPTIONS request.
	RewriteCond %{REQUEST_METHOD} OPTIONS
	RewriteRule ^(.*)$ $1 [R=200,L]
    </Directory>
</VirtualHost>

<VirtualHost *:80>
    ServerName metadata.adtopy.com
   DocumentRoot "${projectPath}/sites/metadata/html"

    <Directory "${projectPath}/sites/metadata/html">
    RewriteEngine on

    	#RewriteCond ${projectPath}/sites/metadata/html/$0 -f
    	#RewriteRule (.*) $1?site=metadata&%{QUERY_STRING} [L]

        #RewriteRule .* index.php?site=metadata&subpath=$0 [QSA,L]
	RewriteCond %{REQUEST_FILENAME} !(\.png|\.jpg|\.gif|\.jpeg|\.bmp)$
	RewriteRule .* index.php?site=metadata&subpath=$0 [QSA,L]


     Options -Indexes +FollowSymLinks -MultiViews
     AllowOverride All
     Order allow,deny
     Allow from all

	 # Always set these headers.
	Header always set Access-Control-Allow-Origin "*"
	Header always set Access-Control-Allow-Methods "POST, GET, OPTIONS, DELETE, PUT"
	Header always set Access-Control-Max-Age "1000"
	Header always set Access-Control-Allow-Headers "x-requested-with, Content-Type, origin, authorization, accept, client-security-token"

	# Added a rewrite to respond with a 200 SUCCESS on every OPTIONS request.
	RewriteCond %{REQUEST_METHOD} OPTIONS
	RewriteRule ^(.*)$ $1 [R=200,L]
    </Directory>
</VirtualHost>
